<h1><%= title %></h1>

<ul>
    <% if (author_list.length === 0) { %>
    <li>There are no authors</li>
    <% } else {%> <% author_list.forEach(author => { %>
    <li>
        <a href="<%= author.url %>"><%= author.name %></a>
        (<%= author.lifespan %>)
    </li>
    <% }) }%>
</ul>
